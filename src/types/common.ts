export enum ERole {
  subscriber = "subscriber",
  serviceProvider = "serviceProvider",
  admin = "admin",
  super_admin = "super_admin",
}

export enum EStatus {
  active = "active",
  deactive = "deactive",
  pending = "pending",
}
export enum EGender {
  male = "male",
  female = "female",
}
export enum TStatus {
  active = "active",
  deactive = "deactive",
  pending = "pending",
  booked = "booked",
  finish = "finish",
}